# Python environment & caches
.venv/
__pycache__/
*.pyc

# Local secrets / sessions — must never be committed
.garminconnect/
*.token
.env
.streamlit/secrets.toml

# Local data cache (personal health data — keep on-device only)
*.db
